import { sqliteTable, text, integer, index, uniqueIndex } from 'drizzle-orm/sqlite-core';
export const classrooms=sqliteTable('classrooms',{id:text('id').primaryKey(),name:text('name').notNull(),ownerId:text('owner_id').notNull(),createdAt:text('created_at').notNull()});
export const members=sqliteTable('members',{email:text('email').primaryKey(),userId:text('user_id'),name:text('name').notNull(),role:text('role').notNull()});
export const students=sqliteTable('students',{id:text('id').primaryKey(),classId:text('class_id').notNull().references(()=>classrooms.id),number:text('number').notNull(),name:text('name').notNull(),group:text('group_name').notNull().default('')},t=>[uniqueIndex('students_class_number').on(t.classId,t.number)]);
export const batches=sqliteTable('batches',{id:text('id').primaryKey(),creatorId:text('creator_id').notNull(),payloadHash:text('payload_hash').notNull(),createdAt:text('created_at').notNull()});
export const entries=sqliteTable('entries',{id:text('id').primaryKey(),batchId:text('batch_id').notNull().references(()=>batches.id),studentId:text('student_id').notNull().references(()=>students.id),title:text('title').notNull(),category:text('category').notNull(),points:integer('points').notNull(),unitPoints:integer('unit_points').notNull().default(1),quantity:integer('quantity').notNull().default(1),date:text('date').notNull(),createdAt:text('created_at').notNull(),operator:text('operator').notNull(),voidedAt:text('voided_at'),voidReason:text('void_reason'),voidedBy:text('voided_by')},t=>[index('entries_date').on(t.date),index('entries_student_date').on(t.studentId,t.date),index('entries_batch').on(t.batchId)]);
