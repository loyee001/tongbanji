'use client';
import { useState, type FormEvent } from 'react';
import { Sprout, ArrowRight } from 'lucide-react';
import { Input } from '@/components/ui/input';
export default function LoginForm(){const [busy,setBusy]=useState(false),[error,setError]=useState('');async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();if(busy)return;const form=new FormData(event.currentTarget);setBusy(true);setError('');try{const response=await fetch('/api/auth',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'login',email:form.get('email'),password:form.get('password')})});const result=await response.json();if(!response.ok)throw new Error(result.error||'登录失败，请重试。');window.location.assign('/')}catch(e){setError(e instanceof Error?e.message:'暂时无法连接服务器。');setBusy(false)}}return <main className="login-shell"><section className="login-card panel"><span className="brand-mark"><Sprout/></span><span className="eyebrow">OUR CLASS, EVERY DAY</span><h1>欢迎回到同班记 <span className="heading-dot">✦</span></h1><p>登录班级登记台，把每一笔分数记清楚。</p><form onSubmit={submit}><label>登录账号<Input name="email" type="email" autoComplete="username" placeholder="管理员或记录员的账号邮箱" required maxLength={160}/></label><label>登录密码<Input name="password" type="password" autoComplete="current-password" required maxLength={128}/></label>{error&&<div className="login-error" role="alert">{error}</div>}<button className="button button-primary" disabled={busy}>{busy?'正在登录…':'进入班级登记台'}<ArrowRight size={17}/></button></form><small>由管理员分配账号 · 支持 1–2 名学生记录员</small></section></main>}
